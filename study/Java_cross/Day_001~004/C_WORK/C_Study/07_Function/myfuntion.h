#pragma once

// 헤더파일 (*.h)
// 함수선언, 매크로, 상수, 타입정의 등을 모아놓은 파일0

const int MAX = 1000;	// 전역 상수

// 타입 정의
// typedef 타입 이름
typedef unsigned int Data;

// 함수 선언
int power(int, int);
double doubleUp(double);
